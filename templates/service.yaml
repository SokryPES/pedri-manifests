apiVersion: v1
kind: Service
metadata:
  name: {{ include "pedri-manifests.fullname" . }}
  namespace: devops-tools
  labels:
    {{- include "pedri-manifests.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "pedri-manifests.selectorLabels" . | nindent 4 }}