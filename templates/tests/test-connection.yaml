apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pedri-manifests.fullname" . }}-test-connection"
  labels:
    {{- include "pedri-manifests.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pedri-manifests.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
